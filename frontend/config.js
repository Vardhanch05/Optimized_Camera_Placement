// Shared frontend sampling/grid constants
export const SAMPLE_STEP = 10;           // default fine sampling step (pixels)
export const COVERAGE_SAMPLE_STEP = 15;  // sampling step used for coverage checks during optimization
export const CANDIDATE_STEP = 30;        // candidate grid step for camera positions
export const GRID_SPACING = 25;          // spacing for translucent canvas grid (pixels)
